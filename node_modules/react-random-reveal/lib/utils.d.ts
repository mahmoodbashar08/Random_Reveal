/// <reference types="react" />
import { Props, Characters } from './types';
export declare const getRandomCharacter: (set?: Characters) => import("react").ReactNode;
/**
 * returns an array of objects where each object contains data for each character
 */
export declare const getCharactersData: ({ characters, duration, revealDuration, revealEasing, ignoreCharacterSet, }: Props) => {
    character: import("react").ReactNode;
    isIgnored: boolean;
    revealTime: number;
}[];
