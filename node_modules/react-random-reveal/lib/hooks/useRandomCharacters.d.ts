import { CharactersRequired, OnComplete, CharactersData } from '../types';
declare type Data = {
    isPlaying: boolean;
    speed: number;
    durationMilliseconds: number;
    characterSet: CharactersRequired;
    onComplete: OnComplete;
    charactersData: CharactersData;
};
export declare const useRandomCharacters: ({ isPlaying, speed, characterSet, onComplete, charactersData, durationMilliseconds, }: Data) => string;
export {};
